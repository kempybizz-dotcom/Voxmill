# Voxmill Market Intelligence — Python Dependencies
# Production-ready PDF generation system

# Core PDF Generation
weasyprint>=60.0.0
Jinja2>=3.1.0
MarkupSafe>=2.1.0

# WeasyPrint Dependencies (pre-installed on most systems)
# These are typically system packages, but included for completeness
# On Render.com, add to build script:
# apt-get update && apt-get install -y libpango-1.0-0 libpangocairo-1.0-0 libgdk-pixbuf2.0-0 libffi-dev shared-mime-info

# Optional but recommended for production
cffi>=1.15.0
Pillow>=10.0.0
tinycss2>=1.2.0
cssselect2>=0.7.0
