# VOXMILL PRODUCTION REQUIREMENTS
# Compatible with Python 3.13
# Complete system: Data Collection → AI Analysis → PDF Generation → Email Delivery
# Core utilities
requests==2.31.0
python-dotenv==1.0.0
python-dateutil==2.8.2
# Data Collection APIs
outscraper==5.9.2
# AI Analysis
openai==1.54.3

# PDF Generation (WeasyPrint - Fortune-500 quality)
weasyprint>=60.0.0
Jinja2>=3.1.0
MarkupSafe>=2.1.0
cffi>=1.15.0
Pillow>=10.4.0
tinycss2>=1.2.0
cssselect2>=0.7.0

# Data visualization (optional - for matplotlib charts if needed)
matplotlib==3.9.2
numpy==2.1.3

# Email (uses stdlib smtplib - no additional packages needed)
# No additional email packages needed (uses stdlib smtplib)
# System dependencies for WeasyPrint (install via apt-get in Render build script):
# apt-get update && apt-get install -y libpango-1.0-0 libpangocairo-1.0-0 libgdk-pixbuf2.0-0 libffi-dev shared-mime-info"""
